import React from 'react';

/**
 * Componente de puntitos animados para indicar carga
 * Muestra tres puntos que aparecen secuencialmente
 */
const LoadingDots = () => {
  return <span className="loading-dots"></span>;
};

export default LoadingDots;
